import { TextStyle } from 'react-native';

// Ported from Helpers/AppFonts.swift
export const fonts = {
  title: { fontSize: 28, fontWeight: '700' } as TextStyle,
  subtitle: { fontSize: 16, fontWeight: '500' } as TextStyle,
};
