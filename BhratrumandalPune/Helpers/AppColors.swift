import SwiftUI

struct AppColors {
    static let primary = Color(red: 0.82, green: 0.11, blue: 0.13) // Matrimony red
    static let background = Color(.systemBackground)
    static let grayText = Color.gray
}
